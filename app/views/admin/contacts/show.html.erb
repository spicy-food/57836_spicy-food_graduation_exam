<<div class="container mt-4">
  <h1 class="mb-4">
    <i class="fas fa-envelope-open-text me-2"></i>お問い合わせ詳細
  </h1>
  <div class="card">
    <div class="card-header">
      <strong>件名：</strong><%= @contact.subject %>
    </div>
    <div class="card-body">
      <p><strong>お名前：</strong><%= @contact.name %></p>
      <p><strong>メールアドレス：</strong><%= @contact.email %></p>
      <p><strong>カテゴリ：</strong><%= @contact.category %></p>
      <p><strong>メッセージ：</strong></p>
      <div class="bg-light p-3 rounded mb-3">
        <%= simple_format(@contact.message) %>
      </div>
      <p class="text-muted"><strong>送信日時：</strong><%= @contact.created_at.strftime("%Y/%m/%d %H:%M") %></p>
      <%= link_to "一覧に戻る", admin_contacts_path, class: "btn btn-outline-secondary" %>
    </div>
  </div>
</div>
<!DOCTYPE html>
<html>
  <head>
    <title>Coffee Records - 管理者</title>
    <meta name="viewport" content="width=device-width,initial-scale=1">
    <%= csrf_meta_tags %>
    <%= csp_meta_tag %>

    <%= favicon_link_tag '/favicon.ico' %>

    <!-- Bootstrap CSS -->
    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0/dist/css/bootstrap.min.css" rel="stylesheet">
    <!-- Font Awesome -->
    <link href="https://cdnjs.cloudflare.com/ajax/libs/font-awesome/5.15.4/css/all.min.css" rel="stylesheet">

    <%= stylesheet_link_tag "application", "data-turbo-track": "reload" %>
    <%= javascript_importmap_tags %>
    <script src="https://cdn.jsdelivr.net/npm/@popperjs/core@2.11.8/dist/umd/popper.min.js"></script>
    <script src="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0/dist/js/bootstrap.min.js"></script>
  </head>

  <body class="d-flex flex-column min-vh-100">
    <nav class="navbar navbar-expand-lg navbar-dark bg-dark mb-3">
      <div class="container">
        <%= link_to root_path, class: "navbar-brand d-flex align-items-center" do %>
          <i class="fas fa-coffee me-2"></i>
          <span class="d-none d-sm-inline">Coffee Records</span>
          <span class="d-inline d-sm-none">CR</span>
        <% end %>
        
        <button class="navbar-toggler" type="button" data-bs-toggle="collapse" data-bs-target="#navbarNav" aria-controls="navbarNav" aria-expanded="false" aria-label="Toggle navigation">
          <span class="navbar-toggler-icon"></span>
        </button>
        
        <div class="collapse navbar-collapse" id="navbarNav">
          <ul class="navbar-nav ms-auto">
            <% if user_signed_in? %>
              <li class="nav-item">
                <%= link_to coffee_records_path, class: "nav-link d-flex align-items-center" do %>
                  <i class="fas fa-list me-1 d-none d-md-inline"></i>
                  <span>コーヒー記録一覧</span>
                <% end %>
              </li>
              <li class="nav-item">
                <%= link_to bookmarks_path, class: "nav-link d-flex align-items-center" do %>
                  <i class="fas fa-bookmark me-1 d-none d-md-inline"></i>
                  <span>ブックマーク一覧</span>
                <% end %>
              </li>
              <li class="nav-item">
                <%= link_to new_coffee_record_path, class: "nav-link d-flex align-items-center" do %>
                  <i class="fas fa-plus me-1 d-none d-md-inline"></i>
                  <span>新規記録</span>
                <% end %>
              </li>
              <li class="nav-item">
                <%= link_to admin_contacts_path, class: "nav-link d-flex align-items-center" do %>
                  <i class="fas fa-cog me-1 d-none d-md-inline"></i>
                  <span>管理者ページ</span>
                <% end %>
              </li>
              <li class="nav-item">
                <%= link_to "ダッシュボード", admin_dashboard_path, class: "nav-link d-flex align-items-center" %>
              </li>    
              <li class="nav-item">
                <%= button_to destroy_user_session_path, method: :delete, class: "btn btn-link nav-link" do %>
                  <i class="fas fa-sign-out-alt me-1 d-none d-md-inline"></i>
                  <span>ログアウト</span>
                <% end %>
              </li>
            <% else %>
              <li class="nav-item">
                <%= link_to new_user_session_path, class: "nav-link d-flex align-items-center" do %>
                  <i class="fas fa-sign-in-alt me-1 d-none d-md-inline"></i>
                  <span>ログイン</span>
                <% end %>
              </li>
              <li class="nav-item">
                <%= link_to new_user_registration_path, class: "nav-link d-flex align-items-center" do %>
                  <i class="fas fa-user-plus me-1 d-none d-md-inline"></i>
                  <span>新規登録</span>
                <% end %>
              </li>
            <% end %>
          </ul>
        </div>
      </div>
    </nav>

    <main class="flex-grow-1">
      <div class="container-fluid">
        <% if notice %>
          <div class="alert alert-success alert-dismissible fade show" role="alert">
            <%= notice %>
            <button type="button" class="btn-close" data-bs-dismiss="alert" aria-label="Close"></button>
          </div>
        <% end %>
        <% if alert %>
          <div class="alert alert-danger alert-dismissible fade show" role="alert">
            <%= alert %>
            <button type="button" class="btn-close" data-bs-dismiss="alert" aria-label="Close"></button>
          </div>
        <% end %>
        
        <%= yield %>
      </div>
    </main>

    <!-- フッター -->
    <footer class="bg-dark text-light mt-5 py-4">
      <div class="container">
        <div class="row">
          <div class="col-md-6">
            <h5>Coffee Records - 管理者</h5>
            <p class="mb-0">コーヒー記録管理システム</p>
          </div>
          <div class="col-md-6 text-md-end">
            <ul class="list-unstyled mb-0">
              <li class="mb-2">
                <%= link_to "利用規約", terms_of_service_path, class: "text-light text-decoration-none" %>
              </li>
              <li class="mb-2">
                <%= link_to "プライバシーポリシー", privacy_policy_path, class: "text-light text-decoration-none" %>
              </li>
              <li class="mb-2">
                <%= link_to "お問い合わせ", contact_path, class: "text-light text-decoration-none" %>
              </li>
              <li>
                <small class="text-muted">&copy; 2024 Coffee Records. All rights reserved.</small>
              </li>
            </ul>
          </div>
        </div>
      </div>
    </footer>
  </body>
</html>